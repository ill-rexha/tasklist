<h1>すべてのタスク</h1>

<ul>
  <% @tasks.each do |task| %>
    <li><%= link_to task.id, task %><%= task.content %></li>
    <li><%= task.status %></li>
    <li>作られたのは【<%= task.created_at %>】です。</li>
  <% end %>
</ul>
<%= link_to '新しいタスクを登録する', new_task_path %>
